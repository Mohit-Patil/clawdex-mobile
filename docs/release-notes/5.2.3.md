- Push notifications: get alerted when an agent turn finishes or needs your approval, even with the app closed.
- Turn-finished notifications show a short preview of the agent's reply.
- Approval notifications include Approve and Deny buttons so you can respond without opening the app.
- Save and reuse prompts from a new prompt library in the composer.
- Notification controls live in Settings, including per-event toggles.
